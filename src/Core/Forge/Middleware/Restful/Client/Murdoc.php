<?php

namespace Core\Forge\Middleware\Restful\Client;

class Murdoc {
	/**
	 * This is going to be a rest api client
	 *
	 * We should support the following functions:
	 * GET
	 * POST
	 * PUT
	 * PATCH
	 * DELETE
	 * COPY
	 * HEAD
	 * OPTIONS
	 * LINK
	 * UNLINK
	 * PURGE
	 * LOCK
	 * UNLOCK
	 * PROPFIND
	 * VIEW
	 *
	 * Typical Features include the ability to manipulate the body, header, pre request scripts, tests.
	 */
}